/** Storefront route paths — use in links to avoid scattered string literals. */
export const storeRoutes = {
  home: "/",
  shop: "/shop",
  login: "/login",
  register: "/register",
  cart: "/cart",
  account: "/account",
  orders: "/orders",
  wishlist: "/wishlist",
} as const;
